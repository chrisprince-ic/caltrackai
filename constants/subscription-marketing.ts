/**
 * In-app marketing copy. Configure the same trial + weekly price in App Store Connect,
 * Google Play, and RevenueCat (intro period + base plan) so store billing matches this UI.
 */
export const SUBSCRIPTION_TRIAL_DAYS = 7;
export const SUBSCRIPTION_WEEKLY_PRICE_USD = 3.99;
export const SUBSCRIPTION_WEEKLY_PRICE_LABEL = '$3.99/week';
