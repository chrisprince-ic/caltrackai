/**
 * Hook placeholder for goal-cross celebrations (confetti, sound).
 * Wire into streak / goals later without touching Energy UI.
 */
export function useGoalCelebration() {
  return {
    celebrateGoalMet: () => {
      /* optional analytics */
    },
  };
}
